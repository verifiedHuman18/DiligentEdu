# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class UserActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.User]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = User.prisma().query_raw(
            'SELECT * FROM User WHERE id = $1',
            'bbadfchfja',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.User
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = User.prisma().query_first(
            'SELECT * FROM User WHERE email = $1',
            'dhheabfhf',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.UserCreateInput,
        include: Optional[types.UserInclude] = None
    ) -> _PrismaModelT:
        """Create a new User record.

        Parameters
        ----------
        data
            User record data
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The created User record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a User record from just the required fields
        user = User.prisma().create(
            data={
                # data to create a User record
                'id': 'ggciceaie',
                'email': 'bbehjachib',
                'role': 'cadfabfehe',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.UserCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple User records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of User record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = User.prisma().create_many(
            data=[
                {
                    # data to create a User record
                    'id': 'dgiiaaijj',
                    'email': 'bfaiacjjfc',
                    'role': 'eigcfgbif',
                },
                {
                    # data to create a User record
                    'id': 'bagcfbhiig',
                    'email': 'cghideieh',
                    'role': 'biabhbdai',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single User record.

        Parameters
        ----------
        where
            User filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The deleted User record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = User.prisma().delete(
            where={
                'id': 'idghgaicb',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique User record.

        Parameters
        ----------
        where
            User filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The found User record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = User.prisma().find_unique(
            where={
                'id': 'fjfddhigg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> _PrismaModelT:
        """Find a unique User record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            User filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The found User record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = User.prisma().find_unique_or_raise(
            where={
                'id': 'hjaecfifb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
        include: Optional[types.UserInclude] = None,
        order: Optional[Union[types.UserOrderByInput, List[types.UserOrderByInput]]] = None,
        distinct: Optional[List[types.UserScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple User records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of User records returned
        skip
            Ignore the first N results
        where
            User filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned User model
        order
            Order the returned User records by any field
        distinct
            Filter User records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.User]
            The list of all User records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 User records
        users = User.prisma().find_many(take=10)

        # find the first 5 User records ordered by the name field
        users = User.prisma().find_many(
            take=5,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
        include: Optional[types.UserInclude] = None,
        order: Optional[Union[types.UserOrderByInput, List[types.UserOrderByInput]]] = None,
        distinct: Optional[List[types.UserScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single User record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            User filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned User model
        order
            Order the returned User records by any field
        distinct
            Filter User records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.User
            The first User record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second User record ordered by the role field
        user = User.prisma().find_first(
            skip=1,
            order={
                'role': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
        include: Optional[types.UserInclude] = None,
        order: Optional[Union[types.UserOrderByInput, List[types.UserOrderByInput]]] = None,
        distinct: Optional[List[types.UserScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single User record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            User filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned User model
        order
            Order the returned User records by any field
        distinct
            Filter User records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.User
            The first User record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second User record ordered by the subject field
        user = User.prisma().find_first_or_raise(
            skip=1,
            order={
                'subject': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.UserUpdateInput,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single User record.

        Parameters
        ----------
        data
            User record data specifying what to update
        where
            User filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The updated User record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = User.prisma().update(
            where={
                'id': 'cbbbjbfcii',
            },
            data={
                # data to update the User record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.UserWhereUniqueInput,
        data: types.UserUpsertInput,
        include: Optional[types.UserInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            User filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The created or updated User record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = User.prisma().upsert(
            where={
                'id': 'bbejhfidcb',
            },
            data={
                'create': {
                    'id': 'bbejhfidcb',
                    'email': 'cghideieh',
                    'role': 'biabhbdai',
                },
                'update': {
                    'email': 'cghideieh',
                    'role': 'biabhbdai',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.UserUpdateManyMutationInput,
        where: types.UserWhereInput,
    ) -> int:
        """Update multiple User records

        Parameters
        ----------
        data
            User data to update the selected User records to
        where
            Filter to select the User records to update

        Returns
        -------
        int
            The total number of User records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all User records
        total = User.prisma().update_many(
            data={
                'class_level': 1644289366
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
    ) -> int:
        """Count the number of User records present in the database

        Parameters
        ----------
        select
            Select the User fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            User filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UserCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = User.prisma().count()

        # results: prisma.types.UserCountAggregateOutput
        results = User.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.UserCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
    ) -> types.UserCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.UserCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
    ) -> Union[int, types.UserCountAggregateOutput]:
        """Count the number of User records present in the database

        Parameters
        ----------
        select
            Select the User fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            User filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UserCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = User.prisma().count()

        # results: prisma.types.UserCountAggregateOutput
        results = User.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.UserCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.UserWhereInput] = None
    ) -> int:
        """Delete multiple User records.

        Parameters
        ----------
        where
            Optional User filter to find the records to be deleted

        Returns
        -------
        int
            The total number of User records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all User records
        total = User.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.UserScalarFieldKeys'],
        *,
        where: Optional['types.UserWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.UserAvgAggregateInput'] = None,
        sum: Optional['types.UserSumAggregateInput'] = None,
        min: Optional['types.UserMinAggregateInput'] = None,
        max: Optional['types.UserMaxAggregateInput'] = None,
        having: Optional['types.UserScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.UserCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.UserScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.UserScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.UserGroupByOutput']:
        """Group User records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar User fields to group records by
        where
            User filter to select records
        take
            Limit the maximum number of User records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.UserGroupByOutput]
            A list of dictionaries representing the User record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group User records by email values
        # and count how many records are in each group
        results = User.prisma().group_by(
            ['email'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class QuizAttemptActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.QuizAttempt]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = QuizAttempt.prisma().query_raw(
            'SELECT * FROM QuizAttempt WHERE quiz_id = $1',
            'bdiicjafbj',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.QuizAttempt
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = QuizAttempt.prisma().query_first(
            'SELECT * FROM QuizAttempt WHERE student_id = $1',
            'bgehebiafc',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.QuizAttemptCreateInput,
        include: Optional[types.QuizAttemptInclude] = None
    ) -> _PrismaModelT:
        """Create a new QuizAttempt record.

        Parameters
        ----------
        data
            QuizAttempt record data
        include
            Specifies which relations should be loaded on the returned QuizAttempt model

        Returns
        -------
        prisma.models.QuizAttempt
            The created QuizAttempt record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a QuizAttempt record from just the required fields
        quizattempt = QuizAttempt.prisma().create(
            data={
                # data to create a QuizAttempt record
                'quiz_id': 'bghffegacj',
                'student_id': 'bhghchehcc',
                'class_level': 326272115,
                'chapter': 'bdedcabahc',
                'difficulty': 'ghfhiafcb',
                'score': 744964398,
                'total_questions': 1969681615,
                'percentage': 1116175964.86147,
                'timestamp': 'bdadaadhag',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.QuizAttemptCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple QuizAttempt records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of QuizAttempt record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = QuizAttempt.prisma().create_many(
            data=[
                {
                    # data to create a QuizAttempt record
                    'quiz_id': 'bgiggdidbf',
                    'student_id': 'caaaedabfc',
                    'class_level': 1868141281,
                    'chapter': 'bigaiehgcc',
                    'difficulty': 'beeifcbebf',
                    'score': 1628650740,
                    'total_questions': 1249606685,
                    'percentage': 835903122.76371,
                    'timestamp': 'ecjjjfbae',
                },
                {
                    # data to create a QuizAttempt record
                    'quiz_id': 'bhhfibbigf',
                    'student_id': 'ijdbeffgg',
                    'class_level': 995405759,
                    'chapter': 'cbachdgfce',
                    'difficulty': 'chbfcacbd',
                    'score': 456633834,
                    'total_questions': 2058258651,
                    'percentage': 1583689592.87844,
                    'timestamp': 'bghfciaafe',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.QuizAttemptWhereUniqueInput,
        include: Optional[types.QuizAttemptInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single QuizAttempt record.

        Parameters
        ----------
        where
            QuizAttempt filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned QuizAttempt model

        Returns
        -------
        prisma.models.QuizAttempt
            The deleted QuizAttempt record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        quizattempt = QuizAttempt.prisma().delete(
            where={
                'quiz_id': 'bgchfhgceh',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.QuizAttemptWhereUniqueInput,
        include: Optional[types.QuizAttemptInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique QuizAttempt record.

        Parameters
        ----------
        where
            QuizAttempt filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned QuizAttempt model

        Returns
        -------
        prisma.models.QuizAttempt
            The found QuizAttempt record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        quizattempt = QuizAttempt.prisma().find_unique(
            where={
                'quiz_id': 'cafeiaccbc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.QuizAttemptWhereUniqueInput,
        include: Optional[types.QuizAttemptInclude] = None
    ) -> _PrismaModelT:
        """Find a unique QuizAttempt record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            QuizAttempt filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned QuizAttempt model

        Returns
        -------
        prisma.models.QuizAttempt
            The found QuizAttempt record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        quizattempt = QuizAttempt.prisma().find_unique_or_raise(
            where={
                'quiz_id': 'gaddfhfh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.QuizAttemptWhereInput] = None,
        cursor: Optional[types.QuizAttemptWhereUniqueInput] = None,
        include: Optional[types.QuizAttemptInclude] = None,
        order: Optional[Union[types.QuizAttemptOrderByInput, List[types.QuizAttemptOrderByInput]]] = None,
        distinct: Optional[List[types.QuizAttemptScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple QuizAttempt records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of QuizAttempt records returned
        skip
            Ignore the first N results
        where
            QuizAttempt filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned QuizAttempt model
        order
            Order the returned QuizAttempt records by any field
        distinct
            Filter QuizAttempt records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.QuizAttempt]
            The list of all QuizAttempt records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 QuizAttempt records
        quizattempts = QuizAttempt.prisma().find_many(take=10)

        # find the first 5 QuizAttempt records ordered by the class_level field
        quizattempts = QuizAttempt.prisma().find_many(
            take=5,
            order={
                'class_level': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.QuizAttemptWhereInput] = None,
        cursor: Optional[types.QuizAttemptWhereUniqueInput] = None,
        include: Optional[types.QuizAttemptInclude] = None,
        order: Optional[Union[types.QuizAttemptOrderByInput, List[types.QuizAttemptOrderByInput]]] = None,
        distinct: Optional[List[types.QuizAttemptScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single QuizAttempt record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            QuizAttempt filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned QuizAttempt model
        order
            Order the returned QuizAttempt records by any field
        distinct
            Filter QuizAttempt records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.QuizAttempt
            The first QuizAttempt record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second QuizAttempt record ordered by the subject field
        quizattempt = QuizAttempt.prisma().find_first(
            skip=1,
            order={
                'subject': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.QuizAttemptWhereInput] = None,
        cursor: Optional[types.QuizAttemptWhereUniqueInput] = None,
        include: Optional[types.QuizAttemptInclude] = None,
        order: Optional[Union[types.QuizAttemptOrderByInput, List[types.QuizAttemptOrderByInput]]] = None,
        distinct: Optional[List[types.QuizAttemptScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single QuizAttempt record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            QuizAttempt filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned QuizAttempt model
        order
            Order the returned QuizAttempt records by any field
        distinct
            Filter QuizAttempt records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.QuizAttempt
            The first QuizAttempt record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second QuizAttempt record ordered by the chapter field
        quizattempt = QuizAttempt.prisma().find_first_or_raise(
            skip=1,
            order={
                'chapter': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.QuizAttemptUpdateInput,
        where: types.QuizAttemptWhereUniqueInput,
        include: Optional[types.QuizAttemptInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single QuizAttempt record.

        Parameters
        ----------
        data
            QuizAttempt record data specifying what to update
        where
            QuizAttempt filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned QuizAttempt model

        Returns
        -------
        prisma.models.QuizAttempt
            The updated QuizAttempt record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        quizattempt = QuizAttempt.prisma().update(
            where={
                'quiz_id': 'gieegcbeg',
            },
            data={
                # data to update the QuizAttempt record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.QuizAttemptWhereUniqueInput,
        data: types.QuizAttemptUpsertInput,
        include: Optional[types.QuizAttemptInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            QuizAttempt filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned QuizAttempt model

        Returns
        -------
        prisma.models.QuizAttempt
            The created or updated QuizAttempt record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        quizattempt = QuizAttempt.prisma().upsert(
            where={
                'quiz_id': 'bgcffadich',
            },
            data={
                'create': {
                    'quiz_id': 'bgcffadich',
                    'student_id': 'ijdbeffgg',
                    'class_level': 995405759,
                    'chapter': 'cbachdgfce',
                    'difficulty': 'chbfcacbd',
                    'score': 456633834,
                    'total_questions': 2058258651,
                    'percentage': 1583689592.87844,
                    'timestamp': 'bghfciaafe',
                },
                'update': {
                    'student_id': 'ijdbeffgg',
                    'class_level': 995405759,
                    'chapter': 'cbachdgfce',
                    'difficulty': 'chbfcacbd',
                    'score': 456633834,
                    'total_questions': 2058258651,
                    'percentage': 1583689592.87844,
                    'timestamp': 'bghfciaafe',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.QuizAttemptUpdateManyMutationInput,
        where: types.QuizAttemptWhereInput,
    ) -> int:
        """Update multiple QuizAttempt records

        Parameters
        ----------
        data
            QuizAttempt data to update the selected QuizAttempt records to
        where
            Filter to select the QuizAttempt records to update

        Returns
        -------
        int
            The total number of QuizAttempt records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all QuizAttempt records
        total = QuizAttempt.prisma().update_many(
            data={
                'chapter_number': 521827728
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.QuizAttemptWhereInput] = None,
        cursor: Optional[types.QuizAttemptWhereUniqueInput] = None,
    ) -> int:
        """Count the number of QuizAttempt records present in the database

        Parameters
        ----------
        select
            Select the QuizAttempt fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            QuizAttempt filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.QuizAttemptCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = QuizAttempt.prisma().count()

        # results: prisma.types.QuizAttemptCountAggregateOutput
        results = QuizAttempt.prisma().count(
            select={
                '_all': True,
                'difficulty': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.QuizAttemptCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.QuizAttemptWhereInput] = None,
        cursor: Optional[types.QuizAttemptWhereUniqueInput] = None,
    ) -> types.QuizAttemptCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.QuizAttemptCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.QuizAttemptWhereInput] = None,
        cursor: Optional[types.QuizAttemptWhereUniqueInput] = None,
    ) -> Union[int, types.QuizAttemptCountAggregateOutput]:
        """Count the number of QuizAttempt records present in the database

        Parameters
        ----------
        select
            Select the QuizAttempt fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            QuizAttempt filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.QuizAttemptCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = QuizAttempt.prisma().count()

        # results: prisma.types.QuizAttemptCountAggregateOutput
        results = QuizAttempt.prisma().count(
            select={
                '_all': True,
                'score': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.QuizAttemptCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.QuizAttemptWhereInput] = None
    ) -> int:
        """Delete multiple QuizAttempt records.

        Parameters
        ----------
        where
            Optional QuizAttempt filter to find the records to be deleted

        Returns
        -------
        int
            The total number of QuizAttempt records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all QuizAttempt records
        total = QuizAttempt.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.QuizAttemptScalarFieldKeys'],
        *,
        where: Optional['types.QuizAttemptWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.QuizAttemptAvgAggregateInput'] = None,
        sum: Optional['types.QuizAttemptSumAggregateInput'] = None,
        min: Optional['types.QuizAttemptMinAggregateInput'] = None,
        max: Optional['types.QuizAttemptMaxAggregateInput'] = None,
        having: Optional['types.QuizAttemptScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.QuizAttemptCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.QuizAttemptScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.QuizAttemptScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.QuizAttemptGroupByOutput']:
        """Group QuizAttempt records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar QuizAttempt fields to group records by
        where
            QuizAttempt filter to select records
        take
            Limit the maximum number of QuizAttempt records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.QuizAttemptGroupByOutput]
            A list of dictionaries representing the QuizAttempt record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group QuizAttempt records by total_questions values
        # and count how many records are in each group
        results = QuizAttempt.prisma().group_by(
            ['total_questions'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class QuestionResponseActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.QuestionResponse]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = QuestionResponse.prisma().query_raw(
            'SELECT * FROM QuestionResponse WHERE id = $1',
            1266032265,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.QuestionResponse
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = QuestionResponse.prisma().query_first(
            'SELECT * FROM QuestionResponse WHERE quiz_id = $1',
            'jdcfdcgc',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.QuestionResponseCreateInput,
        include: Optional[types.QuestionResponseInclude] = None
    ) -> _PrismaModelT:
        """Create a new QuestionResponse record.

        Parameters
        ----------
        data
            QuestionResponse record data
        include
            Specifies which relations should be loaded on the returned QuestionResponse model

        Returns
        -------
        prisma.models.QuestionResponse
            The created QuestionResponse record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a QuestionResponse record from just the required fields
        questionresponse = QuestionResponse.prisma().create(
            data={
                # data to create a QuestionResponse record
                'quiz_id': 'cafdaehjid',
                'question_id': 'gifdddbia',
                'chapter': 'bchehecef',
                'difficulty': 'jeijcbhfe',
                'is_correct': 1964990155,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.QuestionResponseCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple QuestionResponse records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of QuestionResponse record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = QuestionResponse.prisma().create_many(
            data=[
                {
                    # data to create a QuestionResponse record
                    'quiz_id': 'bcciijbibg',
                    'question_id': 'cffcachfd',
                    'chapter': 'bccdfhdigc',
                    'difficulty': 'febcgjbfj',
                    'is_correct': 1064846676,
                },
                {
                    # data to create a QuestionResponse record
                    'quiz_id': 'faidicegb',
                    'question_id': 'bacecgfhbe',
                    'chapter': 'ihcahiead',
                    'difficulty': 'biheheiajg',
                    'is_correct': 916896761,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.QuestionResponseWhereUniqueInput,
        include: Optional[types.QuestionResponseInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single QuestionResponse record.

        Parameters
        ----------
        where
            QuestionResponse filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned QuestionResponse model

        Returns
        -------
        prisma.models.QuestionResponse
            The deleted QuestionResponse record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        questionresponse = QuestionResponse.prisma().delete(
            where={
                'id': 769267518,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.QuestionResponseWhereUniqueInput,
        include: Optional[types.QuestionResponseInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique QuestionResponse record.

        Parameters
        ----------
        where
            QuestionResponse filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned QuestionResponse model

        Returns
        -------
        prisma.models.QuestionResponse
            The found QuestionResponse record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        questionresponse = QuestionResponse.prisma().find_unique(
            where={
                'id': 820312479,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.QuestionResponseWhereUniqueInput,
        include: Optional[types.QuestionResponseInclude] = None
    ) -> _PrismaModelT:
        """Find a unique QuestionResponse record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            QuestionResponse filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned QuestionResponse model

        Returns
        -------
        prisma.models.QuestionResponse
            The found QuestionResponse record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        questionresponse = QuestionResponse.prisma().find_unique_or_raise(
            where={
                'id': 92728044,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.QuestionResponseWhereInput] = None,
        cursor: Optional[types.QuestionResponseWhereUniqueInput] = None,
        include: Optional[types.QuestionResponseInclude] = None,
        order: Optional[Union[types.QuestionResponseOrderByInput, List[types.QuestionResponseOrderByInput]]] = None,
        distinct: Optional[List[types.QuestionResponseScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple QuestionResponse records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of QuestionResponse records returned
        skip
            Ignore the first N results
        where
            QuestionResponse filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned QuestionResponse model
        order
            Order the returned QuestionResponse records by any field
        distinct
            Filter QuestionResponse records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.QuestionResponse]
            The list of all QuestionResponse records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 QuestionResponse records
        questionresponses = QuestionResponse.prisma().find_many(take=10)

        # find the first 5 QuestionResponse records ordered by the question_id field
        questionresponses = QuestionResponse.prisma().find_many(
            take=5,
            order={
                'question_id': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.QuestionResponseWhereInput] = None,
        cursor: Optional[types.QuestionResponseWhereUniqueInput] = None,
        include: Optional[types.QuestionResponseInclude] = None,
        order: Optional[Union[types.QuestionResponseOrderByInput, List[types.QuestionResponseOrderByInput]]] = None,
        distinct: Optional[List[types.QuestionResponseScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single QuestionResponse record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            QuestionResponse filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned QuestionResponse model
        order
            Order the returned QuestionResponse records by any field
        distinct
            Filter QuestionResponse records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.QuestionResponse
            The first QuestionResponse record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second QuestionResponse record ordered by the question_text field
        questionresponse = QuestionResponse.prisma().find_first(
            skip=1,
            order={
                'question_text': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.QuestionResponseWhereInput] = None,
        cursor: Optional[types.QuestionResponseWhereUniqueInput] = None,
        include: Optional[types.QuestionResponseInclude] = None,
        order: Optional[Union[types.QuestionResponseOrderByInput, List[types.QuestionResponseOrderByInput]]] = None,
        distinct: Optional[List[types.QuestionResponseScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single QuestionResponse record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            QuestionResponse filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned QuestionResponse model
        order
            Order the returned QuestionResponse records by any field
        distinct
            Filter QuestionResponse records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.QuestionResponse
            The first QuestionResponse record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second QuestionResponse record ordered by the chapter field
        questionresponse = QuestionResponse.prisma().find_first_or_raise(
            skip=1,
            order={
                'chapter': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.QuestionResponseUpdateInput,
        where: types.QuestionResponseWhereUniqueInput,
        include: Optional[types.QuestionResponseInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single QuestionResponse record.

        Parameters
        ----------
        data
            QuestionResponse record data specifying what to update
        where
            QuestionResponse filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned QuestionResponse model

        Returns
        -------
        prisma.models.QuestionResponse
            The updated QuestionResponse record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        questionresponse = QuestionResponse.prisma().update(
            where={
                'id': 344858293,
            },
            data={
                # data to update the QuestionResponse record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.QuestionResponseWhereUniqueInput,
        data: types.QuestionResponseUpsertInput,
        include: Optional[types.QuestionResponseInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            QuestionResponse filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned QuestionResponse model

        Returns
        -------
        prisma.models.QuestionResponse
            The created or updated QuestionResponse record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        questionresponse = QuestionResponse.prisma().upsert(
            where={
                'id': 1121741130,
            },
            data={
                'create': {
                    'id': 1121741130,
                    'quiz_id': 'faidicegb',
                    'question_id': 'bacecgfhbe',
                    'chapter': 'ihcahiead',
                    'difficulty': 'biheheiajg',
                    'is_correct': 916896761,
                },
                'update': {
                    'quiz_id': 'faidicegb',
                    'question_id': 'bacecgfhbe',
                    'chapter': 'ihcahiead',
                    'difficulty': 'biheheiajg',
                    'is_correct': 916896761,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.QuestionResponseUpdateManyMutationInput,
        where: types.QuestionResponseWhereInput,
    ) -> int:
        """Update multiple QuestionResponse records

        Parameters
        ----------
        data
            QuestionResponse data to update the selected QuestionResponse records to
        where
            Filter to select the QuestionResponse records to update

        Returns
        -------
        int
            The total number of QuestionResponse records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all QuestionResponse records
        total = QuestionResponse.prisma().update_many(
            data={
                'difficulty': 'bejfijgcfb'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.QuestionResponseWhereInput] = None,
        cursor: Optional[types.QuestionResponseWhereUniqueInput] = None,
    ) -> int:
        """Count the number of QuestionResponse records present in the database

        Parameters
        ----------
        select
            Select the QuestionResponse fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            QuestionResponse filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.QuestionResponseCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = QuestionResponse.prisma().count()

        # results: prisma.types.QuestionResponseCountAggregateOutput
        results = QuestionResponse.prisma().count(
            select={
                '_all': True,
                'user_answer': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.QuestionResponseCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.QuestionResponseWhereInput] = None,
        cursor: Optional[types.QuestionResponseWhereUniqueInput] = None,
    ) -> types.QuestionResponseCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.QuestionResponseCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.QuestionResponseWhereInput] = None,
        cursor: Optional[types.QuestionResponseWhereUniqueInput] = None,
    ) -> Union[int, types.QuestionResponseCountAggregateOutput]:
        """Count the number of QuestionResponse records present in the database

        Parameters
        ----------
        select
            Select the QuestionResponse fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            QuestionResponse filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.QuestionResponseCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = QuestionResponse.prisma().count()

        # results: prisma.types.QuestionResponseCountAggregateOutput
        results = QuestionResponse.prisma().count(
            select={
                '_all': True,
                'correct_answer': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.QuestionResponseCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.QuestionResponseWhereInput] = None
    ) -> int:
        """Delete multiple QuestionResponse records.

        Parameters
        ----------
        where
            Optional QuestionResponse filter to find the records to be deleted

        Returns
        -------
        int
            The total number of QuestionResponse records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all QuestionResponse records
        total = QuestionResponse.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.QuestionResponseScalarFieldKeys'],
        *,
        where: Optional['types.QuestionResponseWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.QuestionResponseAvgAggregateInput'] = None,
        sum: Optional['types.QuestionResponseSumAggregateInput'] = None,
        min: Optional['types.QuestionResponseMinAggregateInput'] = None,
        max: Optional['types.QuestionResponseMaxAggregateInput'] = None,
        having: Optional['types.QuestionResponseScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.QuestionResponseCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.QuestionResponseScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.QuestionResponseScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.QuestionResponseGroupByOutput']:
        """Group QuestionResponse records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar QuestionResponse fields to group records by
        where
            QuestionResponse filter to select records
        take
            Limit the maximum number of QuestionResponse records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.QuestionResponseGroupByOutput]
            A list of dictionaries representing the QuestionResponse record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group QuestionResponse records by is_correct values
        # and count how many records are in each group
        results = QuestionResponse.prisma().group_by(
            ['is_correct'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class TeacherActionPlanActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.TeacherActionPlan]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = TeacherActionPlan.prisma().query_raw(
            'SELECT * FROM TeacherActionPlan WHERE id = $1',
            208521688,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.TeacherActionPlan
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = TeacherActionPlan.prisma().query_first(
            'SELECT * FROM TeacherActionPlan WHERE student_id = $1',
            'igaibbfgj',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.TeacherActionPlanCreateInput,
        include: Optional[types.TeacherActionPlanInclude] = None
    ) -> _PrismaModelT:
        """Create a new TeacherActionPlan record.

        Parameters
        ----------
        data
            TeacherActionPlan record data
        include
            Specifies which relations should be loaded on the returned TeacherActionPlan model

        Returns
        -------
        prisma.models.TeacherActionPlan
            The created TeacherActionPlan record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a TeacherActionPlan record from just the required fields
        teacheractionplan = TeacherActionPlan.prisma().create(
            data={
                # data to create a TeacherActionPlan record
                'student_id': 'bggajdcbbi',
                'class_level': 525761943,
                'plan_data': 'hdgcajhjg',
                'updated_at': 'ejdjahicb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.TeacherActionPlanCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple TeacherActionPlan records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of TeacherActionPlan record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = TeacherActionPlan.prisma().create_many(
            data=[
                {
                    # data to create a TeacherActionPlan record
                    'student_id': 'gdjgigfgc',
                    'class_level': 654007347,
                    'plan_data': 'bjafcgbffc',
                    'updated_at': 'hihegjif',
                },
                {
                    # data to create a TeacherActionPlan record
                    'student_id': 'bdjidcidac',
                    'class_level': 856000655,
                    'plan_data': 'befcddgjce',
                    'updated_at': 'bfhdbjjgfd',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.TeacherActionPlanWhereUniqueInput,
        include: Optional[types.TeacherActionPlanInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single TeacherActionPlan record.

        Parameters
        ----------
        where
            TeacherActionPlan filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned TeacherActionPlan model

        Returns
        -------
        prisma.models.TeacherActionPlan
            The deleted TeacherActionPlan record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        teacheractionplan = TeacherActionPlan.prisma().delete(
            where={
                'id': 2013903098,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.TeacherActionPlanWhereUniqueInput,
        include: Optional[types.TeacherActionPlanInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique TeacherActionPlan record.

        Parameters
        ----------
        where
            TeacherActionPlan filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TeacherActionPlan model

        Returns
        -------
        prisma.models.TeacherActionPlan
            The found TeacherActionPlan record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        teacheractionplan = TeacherActionPlan.prisma().find_unique(
            where={
                'id': 500965035,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.TeacherActionPlanWhereUniqueInput,
        include: Optional[types.TeacherActionPlanInclude] = None
    ) -> _PrismaModelT:
        """Find a unique TeacherActionPlan record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            TeacherActionPlan filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TeacherActionPlan model

        Returns
        -------
        prisma.models.TeacherActionPlan
            The found TeacherActionPlan record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        teacheractionplan = TeacherActionPlan.prisma().find_unique_or_raise(
            where={
                'id': 1800624392,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TeacherActionPlanWhereInput] = None,
        cursor: Optional[types.TeacherActionPlanWhereUniqueInput] = None,
        include: Optional[types.TeacherActionPlanInclude] = None,
        order: Optional[Union[types.TeacherActionPlanOrderByInput, List[types.TeacherActionPlanOrderByInput]]] = None,
        distinct: Optional[List[types.TeacherActionPlanScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple TeacherActionPlan records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of TeacherActionPlan records returned
        skip
            Ignore the first N results
        where
            TeacherActionPlan filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TeacherActionPlan model
        order
            Order the returned TeacherActionPlan records by any field
        distinct
            Filter TeacherActionPlan records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.TeacherActionPlan]
            The list of all TeacherActionPlan records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 TeacherActionPlan records
        teacheractionplans = TeacherActionPlan.prisma().find_many(take=10)

        # find the first 5 TeacherActionPlan records ordered by the class_level field
        teacheractionplans = TeacherActionPlan.prisma().find_many(
            take=5,
            order={
                'class_level': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TeacherActionPlanWhereInput] = None,
        cursor: Optional[types.TeacherActionPlanWhereUniqueInput] = None,
        include: Optional[types.TeacherActionPlanInclude] = None,
        order: Optional[Union[types.TeacherActionPlanOrderByInput, List[types.TeacherActionPlanOrderByInput]]] = None,
        distinct: Optional[List[types.TeacherActionPlanScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single TeacherActionPlan record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TeacherActionPlan filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TeacherActionPlan model
        order
            Order the returned TeacherActionPlan records by any field
        distinct
            Filter TeacherActionPlan records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TeacherActionPlan
            The first TeacherActionPlan record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TeacherActionPlan record ordered by the subject field
        teacheractionplan = TeacherActionPlan.prisma().find_first(
            skip=1,
            order={
                'subject': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TeacherActionPlanWhereInput] = None,
        cursor: Optional[types.TeacherActionPlanWhereUniqueInput] = None,
        include: Optional[types.TeacherActionPlanInclude] = None,
        order: Optional[Union[types.TeacherActionPlanOrderByInput, List[types.TeacherActionPlanOrderByInput]]] = None,
        distinct: Optional[List[types.TeacherActionPlanScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single TeacherActionPlan record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TeacherActionPlan filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TeacherActionPlan model
        order
            Order the returned TeacherActionPlan records by any field
        distinct
            Filter TeacherActionPlan records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TeacherActionPlan
            The first TeacherActionPlan record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TeacherActionPlan record ordered by the plan_data field
        teacheractionplan = TeacherActionPlan.prisma().find_first_or_raise(
            skip=1,
            order={
                'plan_data': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.TeacherActionPlanUpdateInput,
        where: types.TeacherActionPlanWhereUniqueInput,
        include: Optional[types.TeacherActionPlanInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single TeacherActionPlan record.

        Parameters
        ----------
        data
            TeacherActionPlan record data specifying what to update
        where
            TeacherActionPlan filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned TeacherActionPlan model

        Returns
        -------
        prisma.models.TeacherActionPlan
            The updated TeacherActionPlan record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        teacheractionplan = TeacherActionPlan.prisma().update(
            where={
                'id': 2077067425,
            },
            data={
                # data to update the TeacherActionPlan record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.TeacherActionPlanWhereUniqueInput,
        data: types.TeacherActionPlanUpsertInput,
        include: Optional[types.TeacherActionPlanInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            TeacherActionPlan filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned TeacherActionPlan model

        Returns
        -------
        prisma.models.TeacherActionPlan
            The created or updated TeacherActionPlan record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        teacheractionplan = TeacherActionPlan.prisma().upsert(
            where={
                'id': 1672112838,
            },
            data={
                'create': {
                    'id': 1672112838,
                    'student_id': 'bdjidcidac',
                    'class_level': 856000655,
                    'plan_data': 'befcddgjce',
                    'updated_at': 'bfhdbjjgfd',
                },
                'update': {
                    'student_id': 'bdjidcidac',
                    'class_level': 856000655,
                    'plan_data': 'befcddgjce',
                    'updated_at': 'bfhdbjjgfd',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.TeacherActionPlanUpdateManyMutationInput,
        where: types.TeacherActionPlanWhereInput,
    ) -> int:
        """Update multiple TeacherActionPlan records

        Parameters
        ----------
        data
            TeacherActionPlan data to update the selected TeacherActionPlan records to
        where
            Filter to select the TeacherActionPlan records to update

        Returns
        -------
        int
            The total number of TeacherActionPlan records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all TeacherActionPlan records
        total = TeacherActionPlan.prisma().update_many(
            data={
                'teacher_notes': 'jcgghhgdj'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TeacherActionPlanWhereInput] = None,
        cursor: Optional[types.TeacherActionPlanWhereUniqueInput] = None,
    ) -> int:
        """Count the number of TeacherActionPlan records present in the database

        Parameters
        ----------
        select
            Select the TeacherActionPlan fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TeacherActionPlan filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TeacherActionPlanCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = TeacherActionPlan.prisma().count()

        # results: prisma.types.TeacherActionPlanCountAggregateOutput
        results = TeacherActionPlan.prisma().count(
            select={
                '_all': True,
                'updated_at': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.TeacherActionPlanCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TeacherActionPlanWhereInput] = None,
        cursor: Optional[types.TeacherActionPlanWhereUniqueInput] = None,
    ) -> types.TeacherActionPlanCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.TeacherActionPlanCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TeacherActionPlanWhereInput] = None,
        cursor: Optional[types.TeacherActionPlanWhereUniqueInput] = None,
    ) -> Union[int, types.TeacherActionPlanCountAggregateOutput]:
        """Count the number of TeacherActionPlan records present in the database

        Parameters
        ----------
        select
            Select the TeacherActionPlan fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TeacherActionPlan filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TeacherActionPlanCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = TeacherActionPlan.prisma().count()

        # results: prisma.types.TeacherActionPlanCountAggregateOutput
        results = TeacherActionPlan.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.TeacherActionPlanCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.TeacherActionPlanWhereInput] = None
    ) -> int:
        """Delete multiple TeacherActionPlan records.

        Parameters
        ----------
        where
            Optional TeacherActionPlan filter to find the records to be deleted

        Returns
        -------
        int
            The total number of TeacherActionPlan records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all TeacherActionPlan records
        total = TeacherActionPlan.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.TeacherActionPlanScalarFieldKeys'],
        *,
        where: Optional['types.TeacherActionPlanWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.TeacherActionPlanAvgAggregateInput'] = None,
        sum: Optional['types.TeacherActionPlanSumAggregateInput'] = None,
        min: Optional['types.TeacherActionPlanMinAggregateInput'] = None,
        max: Optional['types.TeacherActionPlanMaxAggregateInput'] = None,
        having: Optional['types.TeacherActionPlanScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.TeacherActionPlanCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.TeacherActionPlanScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.TeacherActionPlanScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.TeacherActionPlanGroupByOutput']:
        """Group TeacherActionPlan records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar TeacherActionPlan fields to group records by
        where
            TeacherActionPlan filter to select records
        take
            Limit the maximum number of TeacherActionPlan records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.TeacherActionPlanGroupByOutput]
            A list of dictionaries representing the TeacherActionPlan record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group TeacherActionPlan records by student_id values
        # and count how many records are in each group
        results = TeacherActionPlan.prisma().group_by(
            ['student_id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class UploadedDocumentActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.UploadedDocument]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = UploadedDocument.prisma().query_raw(
            'SELECT * FROM UploadedDocument WHERE document_id = $1',
            'beehgcebbg',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.UploadedDocument
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = UploadedDocument.prisma().query_first(
            'SELECT * FROM UploadedDocument WHERE student_id = $1',
            'bhdiaidiaf',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.UploadedDocumentCreateInput,
        include: Optional[types.UploadedDocumentInclude] = None
    ) -> _PrismaModelT:
        """Create a new UploadedDocument record.

        Parameters
        ----------
        data
            UploadedDocument record data
        include
            Specifies which relations should be loaded on the returned UploadedDocument model

        Returns
        -------
        prisma.models.UploadedDocument
            The created UploadedDocument record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a UploadedDocument record from just the required fields
        uploadeddocument = UploadedDocument.prisma().create(
            data={
                # data to create a UploadedDocument record
                'document_id': 'deajegcfi',
                'student_id': 'gabahhhjf',
                'filename': 'cjagadcjg',
                'material_name': 'bifficggej',
                'class_level': 1611009182,
                'subject': 'eegghdhjb',
                'status': 'daafgidjg',
                'uploaded_at': 'gdcgcgagj',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.UploadedDocumentCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple UploadedDocument records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of UploadedDocument record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = UploadedDocument.prisma().create_many(
            data=[
                {
                    # data to create a UploadedDocument record
                    'document_id': 'bhceabbgja',
                    'student_id': 'ehabfhegh',
                    'filename': 'bcajcajjbc',
                    'material_name': 'bfdgheeegf',
                    'class_level': 424218998,
                    'subject': 'cbcfgdcdhf',
                    'status': 'fdgjfbhia',
                    'uploaded_at': 'jcehcdchh',
                },
                {
                    # data to create a UploadedDocument record
                    'document_id': 'bgcbjdhjcc',
                    'student_id': 'bieiidcabj',
                    'filename': 'bjcbfcieaa',
                    'material_name': 'cbaaechiej',
                    'class_level': 849140046,
                    'subject': 'jcibfcbhf',
                    'status': 'chdadcaga',
                    'uploaded_at': 'jicieifbh',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.UploadedDocumentWhereUniqueInput,
        include: Optional[types.UploadedDocumentInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single UploadedDocument record.

        Parameters
        ----------
        where
            UploadedDocument filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned UploadedDocument model

        Returns
        -------
        prisma.models.UploadedDocument
            The deleted UploadedDocument record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        uploadeddocument = UploadedDocument.prisma().delete(
            where={
                'document_id': 'fbahdheji',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.UploadedDocumentWhereUniqueInput,
        include: Optional[types.UploadedDocumentInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique UploadedDocument record.

        Parameters
        ----------
        where
            UploadedDocument filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned UploadedDocument model

        Returns
        -------
        prisma.models.UploadedDocument
            The found UploadedDocument record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        uploadeddocument = UploadedDocument.prisma().find_unique(
            where={
                'document_id': 'cbbheiicgh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.UploadedDocumentWhereUniqueInput,
        include: Optional[types.UploadedDocumentInclude] = None
    ) -> _PrismaModelT:
        """Find a unique UploadedDocument record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            UploadedDocument filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned UploadedDocument model

        Returns
        -------
        prisma.models.UploadedDocument
            The found UploadedDocument record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        uploadeddocument = UploadedDocument.prisma().find_unique_or_raise(
            where={
                'document_id': 'beabjeejdg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UploadedDocumentWhereInput] = None,
        cursor: Optional[types.UploadedDocumentWhereUniqueInput] = None,
        include: Optional[types.UploadedDocumentInclude] = None,
        order: Optional[Union[types.UploadedDocumentOrderByInput, List[types.UploadedDocumentOrderByInput]]] = None,
        distinct: Optional[List[types.UploadedDocumentScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple UploadedDocument records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of UploadedDocument records returned
        skip
            Ignore the first N results
        where
            UploadedDocument filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned UploadedDocument model
        order
            Order the returned UploadedDocument records by any field
        distinct
            Filter UploadedDocument records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.UploadedDocument]
            The list of all UploadedDocument records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 UploadedDocument records
        uploadeddocuments = UploadedDocument.prisma().find_many(take=10)

        # find the first 5 UploadedDocument records ordered by the filename field
        uploadeddocuments = UploadedDocument.prisma().find_many(
            take=5,
            order={
                'filename': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UploadedDocumentWhereInput] = None,
        cursor: Optional[types.UploadedDocumentWhereUniqueInput] = None,
        include: Optional[types.UploadedDocumentInclude] = None,
        order: Optional[Union[types.UploadedDocumentOrderByInput, List[types.UploadedDocumentOrderByInput]]] = None,
        distinct: Optional[List[types.UploadedDocumentScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single UploadedDocument record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            UploadedDocument filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned UploadedDocument model
        order
            Order the returned UploadedDocument records by any field
        distinct
            Filter UploadedDocument records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.UploadedDocument
            The first UploadedDocument record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second UploadedDocument record ordered by the material_name field
        uploadeddocument = UploadedDocument.prisma().find_first(
            skip=1,
            order={
                'material_name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UploadedDocumentWhereInput] = None,
        cursor: Optional[types.UploadedDocumentWhereUniqueInput] = None,
        include: Optional[types.UploadedDocumentInclude] = None,
        order: Optional[Union[types.UploadedDocumentOrderByInput, List[types.UploadedDocumentOrderByInput]]] = None,
        distinct: Optional[List[types.UploadedDocumentScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single UploadedDocument record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            UploadedDocument filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned UploadedDocument model
        order
            Order the returned UploadedDocument records by any field
        distinct
            Filter UploadedDocument records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.UploadedDocument
            The first UploadedDocument record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second UploadedDocument record ordered by the class_level field
        uploadeddocument = UploadedDocument.prisma().find_first_or_raise(
            skip=1,
            order={
                'class_level': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.UploadedDocumentUpdateInput,
        where: types.UploadedDocumentWhereUniqueInput,
        include: Optional[types.UploadedDocumentInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single UploadedDocument record.

        Parameters
        ----------
        data
            UploadedDocument record data specifying what to update
        where
            UploadedDocument filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned UploadedDocument model

        Returns
        -------
        prisma.models.UploadedDocument
            The updated UploadedDocument record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        uploadeddocument = UploadedDocument.prisma().update(
            where={
                'document_id': 'bcjhgahffd',
            },
            data={
                # data to update the UploadedDocument record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.UploadedDocumentWhereUniqueInput,
        data: types.UploadedDocumentUpsertInput,
        include: Optional[types.UploadedDocumentInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            UploadedDocument filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned UploadedDocument model

        Returns
        -------
        prisma.models.UploadedDocument
            The created or updated UploadedDocument record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        uploadeddocument = UploadedDocument.prisma().upsert(
            where={
                'document_id': 'fbjeiiffa',
            },
            data={
                'create': {
                    'document_id': 'fbjeiiffa',
                    'student_id': 'bieiidcabj',
                    'filename': 'bjcbfcieaa',
                    'material_name': 'cbaaechiej',
                    'class_level': 849140046,
                    'subject': 'jcibfcbhf',
                    'status': 'chdadcaga',
                    'uploaded_at': 'jicieifbh',
                },
                'update': {
                    'student_id': 'bieiidcabj',
                    'filename': 'bjcbfcieaa',
                    'material_name': 'cbaaechiej',
                    'class_level': 849140046,
                    'subject': 'jcibfcbhf',
                    'status': 'chdadcaga',
                    'uploaded_at': 'jicieifbh',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.UploadedDocumentUpdateManyMutationInput,
        where: types.UploadedDocumentWhereInput,
    ) -> int:
        """Update multiple UploadedDocument records

        Parameters
        ----------
        data
            UploadedDocument data to update the selected UploadedDocument records to
        where
            Filter to select the UploadedDocument records to update

        Returns
        -------
        int
            The total number of UploadedDocument records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all UploadedDocument records
        total = UploadedDocument.prisma().update_many(
            data={
                'subject': 'jhgidcgbf'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UploadedDocumentWhereInput] = None,
        cursor: Optional[types.UploadedDocumentWhereUniqueInput] = None,
    ) -> int:
        """Count the number of UploadedDocument records present in the database

        Parameters
        ----------
        select
            Select the UploadedDocument fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            UploadedDocument filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UploadedDocumentCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = UploadedDocument.prisma().count()

        # results: prisma.types.UploadedDocumentCountAggregateOutput
        results = UploadedDocument.prisma().count(
            select={
                '_all': True,
                'chapter': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.UploadedDocumentCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UploadedDocumentWhereInput] = None,
        cursor: Optional[types.UploadedDocumentWhereUniqueInput] = None,
    ) -> types.UploadedDocumentCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.UploadedDocumentCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UploadedDocumentWhereInput] = None,
        cursor: Optional[types.UploadedDocumentWhereUniqueInput] = None,
    ) -> Union[int, types.UploadedDocumentCountAggregateOutput]:
        """Count the number of UploadedDocument records present in the database

        Parameters
        ----------
        select
            Select the UploadedDocument fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            UploadedDocument filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UploadedDocumentCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = UploadedDocument.prisma().count()

        # results: prisma.types.UploadedDocumentCountAggregateOutput
        results = UploadedDocument.prisma().count(
            select={
                '_all': True,
                'status': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.UploadedDocumentCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.UploadedDocumentWhereInput] = None
    ) -> int:
        """Delete multiple UploadedDocument records.

        Parameters
        ----------
        where
            Optional UploadedDocument filter to find the records to be deleted

        Returns
        -------
        int
            The total number of UploadedDocument records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all UploadedDocument records
        total = UploadedDocument.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.UploadedDocumentScalarFieldKeys'],
        *,
        where: Optional['types.UploadedDocumentWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.UploadedDocumentAvgAggregateInput'] = None,
        sum: Optional['types.UploadedDocumentSumAggregateInput'] = None,
        min: Optional['types.UploadedDocumentMinAggregateInput'] = None,
        max: Optional['types.UploadedDocumentMaxAggregateInput'] = None,
        having: Optional['types.UploadedDocumentScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.UploadedDocumentCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.UploadedDocumentScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.UploadedDocumentScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.UploadedDocumentGroupByOutput']:
        """Group UploadedDocument records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar UploadedDocument fields to group records by
        where
            UploadedDocument filter to select records
        take
            Limit the maximum number of UploadedDocument records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.UploadedDocumentGroupByOutput]
            A list of dictionaries representing the UploadedDocument record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group UploadedDocument records by error_message values
        # and count how many records are in each group
        results = UploadedDocument.prisma().group_by(
            ['error_message'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class StudyTwinMatchActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.StudyTwinMatch]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = StudyTwinMatch.prisma().query_raw(
            'SELECT * FROM StudyTwinMatch WHERE id = $1',
            1696425492,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.StudyTwinMatch
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = StudyTwinMatch.prisma().query_first(
            'SELECT * FROM StudyTwinMatch WHERE student_id = $1',
            'bgjcgchib',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.StudyTwinMatchCreateInput,
        include: Optional[types.StudyTwinMatchInclude] = None
    ) -> _PrismaModelT:
        """Create a new StudyTwinMatch record.

        Parameters
        ----------
        data
            StudyTwinMatch record data
        include
            Specifies which relations should be loaded on the returned StudyTwinMatch model

        Returns
        -------
        prisma.models.StudyTwinMatch
            The created StudyTwinMatch record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a StudyTwinMatch record from just the required fields
        studytwinmatch = StudyTwinMatch.prisma().create(
            data={
                # data to create a StudyTwinMatch record
                'student_id': 'bacdaibgfa',
                'twin_student_id': 'dchgibach',
                'class_level': 527748992,
                'subject': 'cacjdfhejh',
                'similarity_score': 1318597118.212211,
                'match_data': 'bacejedaca',
                'created_at': 'bhbhdahfaj',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.StudyTwinMatchCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple StudyTwinMatch records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of StudyTwinMatch record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = StudyTwinMatch.prisma().create_many(
            data=[
                {
                    # data to create a StudyTwinMatch record
                    'student_id': 'bfjibceaec',
                    'twin_student_id': 'ibhgcdbgd',
                    'class_level': 1030557336,
                    'subject': 'bbdbfcfihd',
                    'similarity_score': 210666198.127605,
                    'match_data': 'heffgjdei',
                    'created_at': 'dahihgbeb',
                },
                {
                    # data to create a StudyTwinMatch record
                    'student_id': 'bgheaejbcc',
                    'twin_student_id': 'bfcgifeged',
                    'class_level': 958077104,
                    'subject': 'bfbdafajcb',
                    'similarity_score': 204674734.206765,
                    'match_data': 'bbidjbbjaa',
                    'created_at': 'bfijhaejdd',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.StudyTwinMatchWhereUniqueInput,
        include: Optional[types.StudyTwinMatchInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single StudyTwinMatch record.

        Parameters
        ----------
        where
            StudyTwinMatch filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned StudyTwinMatch model

        Returns
        -------
        prisma.models.StudyTwinMatch
            The deleted StudyTwinMatch record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        studytwinmatch = StudyTwinMatch.prisma().delete(
            where={
                'id': 1243475898,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.StudyTwinMatchWhereUniqueInput,
        include: Optional[types.StudyTwinMatchInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique StudyTwinMatch record.

        Parameters
        ----------
        where
            StudyTwinMatch filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned StudyTwinMatch model

        Returns
        -------
        prisma.models.StudyTwinMatch
            The found StudyTwinMatch record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        studytwinmatch = StudyTwinMatch.prisma().find_unique(
            where={
                'id': 1369828971,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.StudyTwinMatchWhereUniqueInput,
        include: Optional[types.StudyTwinMatchInclude] = None
    ) -> _PrismaModelT:
        """Find a unique StudyTwinMatch record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            StudyTwinMatch filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned StudyTwinMatch model

        Returns
        -------
        prisma.models.StudyTwinMatch
            The found StudyTwinMatch record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        studytwinmatch = StudyTwinMatch.prisma().find_unique_or_raise(
            where={
                'id': 1678593480,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StudyTwinMatchWhereInput] = None,
        cursor: Optional[types.StudyTwinMatchWhereUniqueInput] = None,
        include: Optional[types.StudyTwinMatchInclude] = None,
        order: Optional[Union[types.StudyTwinMatchOrderByInput, List[types.StudyTwinMatchOrderByInput]]] = None,
        distinct: Optional[List[types.StudyTwinMatchScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple StudyTwinMatch records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of StudyTwinMatch records returned
        skip
            Ignore the first N results
        where
            StudyTwinMatch filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StudyTwinMatch model
        order
            Order the returned StudyTwinMatch records by any field
        distinct
            Filter StudyTwinMatch records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.StudyTwinMatch]
            The list of all StudyTwinMatch records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 StudyTwinMatch records
        studytwinmatchs = StudyTwinMatch.prisma().find_many(take=10)

        # find the first 5 StudyTwinMatch records ordered by the twin_student_id field
        studytwinmatchs = StudyTwinMatch.prisma().find_many(
            take=5,
            order={
                'twin_student_id': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.StudyTwinMatchWhereInput] = None,
        cursor: Optional[types.StudyTwinMatchWhereUniqueInput] = None,
        include: Optional[types.StudyTwinMatchInclude] = None,
        order: Optional[Union[types.StudyTwinMatchOrderByInput, List[types.StudyTwinMatchOrderByInput]]] = None,
        distinct: Optional[List[types.StudyTwinMatchScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single StudyTwinMatch record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            StudyTwinMatch filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StudyTwinMatch model
        order
            Order the returned StudyTwinMatch records by any field
        distinct
            Filter StudyTwinMatch records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.StudyTwinMatch
            The first StudyTwinMatch record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second StudyTwinMatch record ordered by the class_level field
        studytwinmatch = StudyTwinMatch.prisma().find_first(
            skip=1,
            order={
                'class_level': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.StudyTwinMatchWhereInput] = None,
        cursor: Optional[types.StudyTwinMatchWhereUniqueInput] = None,
        include: Optional[types.StudyTwinMatchInclude] = None,
        order: Optional[Union[types.StudyTwinMatchOrderByInput, List[types.StudyTwinMatchOrderByInput]]] = None,
        distinct: Optional[List[types.StudyTwinMatchScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single StudyTwinMatch record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            StudyTwinMatch filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StudyTwinMatch model
        order
            Order the returned StudyTwinMatch records by any field
        distinct
            Filter StudyTwinMatch records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.StudyTwinMatch
            The first StudyTwinMatch record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second StudyTwinMatch record ordered by the subject field
        studytwinmatch = StudyTwinMatch.prisma().find_first_or_raise(
            skip=1,
            order={
                'subject': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.StudyTwinMatchUpdateInput,
        where: types.StudyTwinMatchWhereUniqueInput,
        include: Optional[types.StudyTwinMatchInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single StudyTwinMatch record.

        Parameters
        ----------
        data
            StudyTwinMatch record data specifying what to update
        where
            StudyTwinMatch filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned StudyTwinMatch model

        Returns
        -------
        prisma.models.StudyTwinMatch
            The updated StudyTwinMatch record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        studytwinmatch = StudyTwinMatch.prisma().update(
            where={
                'id': 403521121,
            },
            data={
                # data to update the StudyTwinMatch record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.StudyTwinMatchWhereUniqueInput,
        data: types.StudyTwinMatchUpsertInput,
        include: Optional[types.StudyTwinMatchInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            StudyTwinMatch filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned StudyTwinMatch model

        Returns
        -------
        prisma.models.StudyTwinMatch
            The created or updated StudyTwinMatch record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        studytwinmatch = StudyTwinMatch.prisma().upsert(
            where={
                'id': 648760710,
            },
            data={
                'create': {
                    'id': 648760710,
                    'student_id': 'bgheaejbcc',
                    'twin_student_id': 'bfcgifeged',
                    'class_level': 958077104,
                    'subject': 'bfbdafajcb',
                    'similarity_score': 204674734.206765,
                    'match_data': 'bbidjbbjaa',
                    'created_at': 'bfijhaejdd',
                },
                'update': {
                    'student_id': 'bgheaejbcc',
                    'twin_student_id': 'bfcgifeged',
                    'class_level': 958077104,
                    'subject': 'bfbdafajcb',
                    'similarity_score': 204674734.206765,
                    'match_data': 'bbidjbbjaa',
                    'created_at': 'bfijhaejdd',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.StudyTwinMatchUpdateManyMutationInput,
        where: types.StudyTwinMatchWhereInput,
    ) -> int:
        """Update multiple StudyTwinMatch records

        Parameters
        ----------
        data
            StudyTwinMatch data to update the selected StudyTwinMatch records to
        where
            Filter to select the StudyTwinMatch records to update

        Returns
        -------
        int
            The total number of StudyTwinMatch records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all StudyTwinMatch records
        total = StudyTwinMatch.prisma().update_many(
            data={
                'similarity_score': 607323719.146889
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StudyTwinMatchWhereInput] = None,
        cursor: Optional[types.StudyTwinMatchWhereUniqueInput] = None,
    ) -> int:
        """Count the number of StudyTwinMatch records present in the database

        Parameters
        ----------
        select
            Select the StudyTwinMatch fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            StudyTwinMatch filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.StudyTwinMatchCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = StudyTwinMatch.prisma().count()

        # results: prisma.types.StudyTwinMatchCountAggregateOutput
        results = StudyTwinMatch.prisma().count(
            select={
                '_all': True,
                'match_data': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.StudyTwinMatchCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StudyTwinMatchWhereInput] = None,
        cursor: Optional[types.StudyTwinMatchWhereUniqueInput] = None,
    ) -> types.StudyTwinMatchCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.StudyTwinMatchCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StudyTwinMatchWhereInput] = None,
        cursor: Optional[types.StudyTwinMatchWhereUniqueInput] = None,
    ) -> Union[int, types.StudyTwinMatchCountAggregateOutput]:
        """Count the number of StudyTwinMatch records present in the database

        Parameters
        ----------
        select
            Select the StudyTwinMatch fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            StudyTwinMatch filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.StudyTwinMatchCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = StudyTwinMatch.prisma().count()

        # results: prisma.types.StudyTwinMatchCountAggregateOutput
        results = StudyTwinMatch.prisma().count(
            select={
                '_all': True,
                'created_at': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.StudyTwinMatchCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.StudyTwinMatchWhereInput] = None
    ) -> int:
        """Delete multiple StudyTwinMatch records.

        Parameters
        ----------
        where
            Optional StudyTwinMatch filter to find the records to be deleted

        Returns
        -------
        int
            The total number of StudyTwinMatch records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all StudyTwinMatch records
        total = StudyTwinMatch.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.StudyTwinMatchScalarFieldKeys'],
        *,
        where: Optional['types.StudyTwinMatchWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.StudyTwinMatchAvgAggregateInput'] = None,
        sum: Optional['types.StudyTwinMatchSumAggregateInput'] = None,
        min: Optional['types.StudyTwinMatchMinAggregateInput'] = None,
        max: Optional['types.StudyTwinMatchMaxAggregateInput'] = None,
        having: Optional['types.StudyTwinMatchScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.StudyTwinMatchCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.StudyTwinMatchScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.StudyTwinMatchScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.StudyTwinMatchGroupByOutput']:
        """Group StudyTwinMatch records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar StudyTwinMatch fields to group records by
        where
            StudyTwinMatch filter to select records
        take
            Limit the maximum number of StudyTwinMatch records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.StudyTwinMatchGroupByOutput]
            A list of dictionaries representing the StudyTwinMatch record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group StudyTwinMatch records by id values
        # and count how many records are in each group
        results = StudyTwinMatch.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models