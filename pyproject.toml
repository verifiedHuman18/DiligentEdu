[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "academic-rag-assistant"
version = "0.1.0"
description = "NCERT Academic Science RAG Assistant & Intelligent Tutor"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=0.3.27",
    "langchain-community>=0.3.29",
    "langchain-google-genai>=2.1.12",
    "langchain-huggingface>=0.3.1",
    "langchain-pinecone>=0.2.12",
    "openai-agents>=0.3.0",
    "pinecone>=7.3.0",
    "plotly>=6.3.0",
    "python-dotenv>=1.1.1",
    "sentence-transformers>=5.1.0",
    "streamlit>=1.49.1",
]

[tool.setuptools.packages.find]
where = ["src"]

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
    "ruff>=0.16.4",
]

[tool.ruff]
target-version = "py312"
line-length = 100
exclude = [
    ".git",
    ".venv",
    "__pycache__",
    "data",
    "logs",
    "notebooks",
]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # Pyflakes
    "I",   # isort
]
ignore = [
    "E501",  # Line too long
    "E402",  # Module level import not at top of file
]

[tool.ruff.lint.isort]
known-first-party = ["src", "backend", "adaptive_engine", "quiz_generator", "quiz_storage", "swat_analyzer", "teacher_engine", "frontend"]

[tool.pyright]
extraPaths = ["."]
